import { View } from 'react-native'
import { ThemedText } from '@/components/ThemedText';
import React from 'react'

const PushApp = () => {
    return (
        <View>
            <ThemedText>PushApp</ThemedText>
        </View>
    );
};

export default PushApp